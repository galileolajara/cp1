require "LibC/time.cp1"
include <sys/stat.h> {
   'c = 'libc
   'x = 'posix
   using 'posix {
      struct 'stat[
         mtimespec'c'timespec. @real-name(st_mtimespec),
         mtim'c'timespec. @real-name(st_mtim),
      ] @real-name(struct stat) @no-decl
      mkdir(path'char[], mode'cint)'cint @real-name @no-decl
      mkdir(path'char[])'cint @real-name @no-decl
      fstat(file'cint, stat'stat)'cint @real-name @no-decl
      stat(file'char[], stat'stat)'cint @real-name @no-decl
   }
}
